<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CodePanel.InfoPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand">
            <Grid VerticalOptions="FillAndExpand">
                <ScrollView VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Margin="5,0,5,0">
                    <Label x:Name="InfoContent" FontSize="24" HorizontalOptions="CenterAndExpand"/>
                </ScrollView>
            </Grid>
            <Button x:Name="NextButton" Style="{StaticResource NavButton}" Text="Ответ" Clicked="NextButton_OnClicked"
                    VerticalOptions="EndAndExpand" IsEnabled="True" FontSize="28"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>